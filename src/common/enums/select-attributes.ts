/**
 * Attributes Array for 'SELECT' clause will setup here
 */

export const SELECT_ATTRIBUTES = Object.freeze({
  APP_DASHBOARD_BY_ID: [
    'user.id',
    'user.username',
    'drop_down_item.id',
    'drop_down_item.label',
    'alarmFilterConfig.id',
    'alarmFilterConfig.alarm_name',
    'alarmFilterConfig.severity',
  ],
});
